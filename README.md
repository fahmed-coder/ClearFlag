# ClearFlag
Verispend is a full-stack transaction analysis system that ingests CSV-based banking statements, classifies high-risk transactions using deterministic rules, and exposes explainable outcomes via a Flask REST API with a decoupled single-page frontend.
